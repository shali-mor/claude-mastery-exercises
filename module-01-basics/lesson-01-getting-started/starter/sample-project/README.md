# Sample Project

A minimal Node.js project used for Claude Code exercises.

## What it does
Processes a list of users with purchases, applies country-specific tax rates, and prints results.

## Run it
```bash
node app.js
```

## Files
- `app.js` — entry point, processes orders
- `utils.js` — `formatGreeting()` and `calculateTax()` helpers

# Parallel Worktrees Prompt

<!-- TODO: Write a single Claude Code message that spawns 3 isolated parallel agents.
     Each agent should:
     - Run on its own worktree (isolated git branch)
     - Target a different API directory (users/, payments/, auth/)
     - Add zod validation to all routes in its directory only
     - Not touch any other directory

     The message should ask Claude to summarise results per agent when all finish.
-->

# Delegation Prompt: Add Input Validation

<!-- TODO: Add the isolation request so the agent runs on its own git branch -->
<!-- Hint: mention "worktree" or "isolated" -->

Use a <!-- TODO: agent type --> agent to <!-- TODO: describe goal -->.

## Context
This is a Next.js 14 app using the App Router.
API routes live in src/app/api/users/.
The `zod` package is already installed.

## Goal
<!-- TODO: Write a specific, measurable goal. What does "done" look like? -->

## Constraints
- <!-- TODO: Scope the agent to one directory only -->
- Do not modify any files outside that directory

## Output Format
<!-- TODO: Specify the output format — what should the agent return when done? -->

## If Blocked
If a route file already has validation, note it and skip it.

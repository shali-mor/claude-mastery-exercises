#!/usr/bin/env bash
# Worktree workflow practice
# Fill in each TODO with the correct git command.
# The branch name is: worktree/add-user-validation
# The worktree path is: .claude/worktrees/agent-abc123

echo "=== Step 1: List all active worktrees ==="
# TODO: run the git command that shows all worktrees and their branches


echo ""
echo "=== Step 2: Review what the agent changed ==="
# TODO: run the git diff command to compare main with the worktree branch
# (show only the branch diff, not unstaged changes)


echo ""
echo "=== Step 3: Navigate into the worktree ==="
# TODO: write the cd command to enter the worktree directory


echo ""
echo "=== Step 4: Merge the worktree branch into main ==="
# (run these from the main branch, not from inside the worktree)
# TODO: switch back to main first


# TODO: merge the worktree branch


echo ""
echo "=== Step 5: Clean up the worktree ==="
# TODO: remove the worktree directory and its branch


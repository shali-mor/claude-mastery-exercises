# Security Review Sub-agent Prompt Template

<!-- TODO: Complete this prompt template so the sub-agent:
1. Knows its exact role (security reviewer for auth module)
2. Knows which files to read
3. Returns output in the exact JSON schema format
4. Wraps the JSON in ```json ... ``` so it's parseable

The prompt should be completely self-contained — the sub-agent has no other context.
-->

You are a security review sub-agent. Your agent_id is "security-reviewer".

TODO: Add the rest of the prompt including:
- What files to read
- What to look for
- The exact JSON output format
- How to wrap the output

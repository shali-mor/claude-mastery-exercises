---
description: Add JSDoc comments to a function
---

# /add-jsdoc

TODO: Write a skill that adds JSDoc comments to the function named $ARGUMENTS.

Requirements:
- Include @param, @returns, and @throws tags
- Do NOT modify the function body or signature
- Use TypeScript types from the source if available
- Place the JSDoc block immediately above the function

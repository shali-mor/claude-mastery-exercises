# Changelog Generator

<!-- TODO: Complete this skill to:
1. Accept a version number via $ARGUMENTS
2. Run `git log --oneline $(git describe --tags --abbrev=0)..HEAD` to get commits since last tag
3. Categorise commits into Added / Changed / Fixed / Breaking sections
4. Output formatted Markdown ready to paste into CHANGELOG.md
-->

Generate a CHANGELOG entry for version $ARGUMENTS:
